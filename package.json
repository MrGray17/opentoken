{
  "name": "opentoken",
  "version": "0.1.0",
  "description": "Token-saving companion for OpenCode — intercepts, filters, and compresses tool outputs",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@opencode-ai/plugin": "*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
