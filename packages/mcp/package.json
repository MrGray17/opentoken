{
	"name": "@opentoken/mcp",
	"version": "1.0.0",
	"description": "MCP server for OpenToken — compress tool output via JSON-RPC over stdio",
	"type": "module",
	"license": "MIT",
	"bin": {
		"opentoken-mcp": "./src/server.ts"
	},
	"engines": {
		"bun": ">=1.2.0"
	},
	"files": [
		"src/**/*.ts"
	],
	"dependencies": {
		"@opentoken/core": "workspace:*"
	},
	"devDependencies": {
		"@types/bun": "^1.3.14",
		"typescript": "^5.9.3"
	}
}
